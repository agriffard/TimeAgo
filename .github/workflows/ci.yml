name: CI

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore library
        run: dotnet restore src/TimeAgochi/TimeAgochi.csproj

      - name: Restore sample
        run: dotnet restore samples/TimeAgochi.Sample/TimeAgochi.Sample.csproj

      - name: Build library
        run: dotnet build src/TimeAgochi/TimeAgochi.csproj -c Release --no-restore

      - name: Build sample
        run: dotnet build samples/TimeAgochi.Sample/TimeAgochi.Sample.csproj -c Release --no-restore

      - name: Pack library
        run: dotnet pack src/TimeAgochi/TimeAgochi.csproj -c Release --no-build --output artifacts/packages

      - name: Upload package artifact
        uses: actions/upload-artifact@v4
        with:
          name: ci-packages
          path: artifacts/packages/*.nupkg
